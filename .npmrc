# Replace OWNER with your GitHub org or username (e.g. @acme or @your-handle).
# The scope in package.json must match this value when you're ready to publish.
@OWNER:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

