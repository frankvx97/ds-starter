# Illustrations

Drop larger narrative artwork here — empty states, onboarding, marketing moments.

- Preferred format: **SVG** (inline or via `<img src>`).
- Fallback: optimized PNG/WebP for complex raster illustrations.
- Naming: kebab-case. Example: `illustration-empty-inbox.svg`.
