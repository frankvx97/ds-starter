# Color

Color tokens live in `../tokens/source/*.tokens.json` (DTCG `$type: "color"`).
Compiled into CSS custom properties — e.g. `var(--color-brand-primary)`.

Put any color-specific helpers (contrast utilities, semantic mapping TS files, etc.)
in this folder as you add them.
