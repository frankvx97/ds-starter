# Typography

Font-family, font-size, line-height, letter-spacing, and font-weight tokens
live in `../tokens/source/` (DTCG `$type: "fontFamily" | "dimension" | "fontWeight"`).

Web fonts go in `../../assets/fonts/` and are registered via `@font-face`
inside `src/styles/global.css`.
