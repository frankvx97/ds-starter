import { Meta } from '@storybook/addon-docs/blocks';
import tokens from './_generated/tokens.json';
import { BlurGrid, TokenTable } from './_components/TokenTable';

<Meta title="Foundations/Blur" />

# Blur

Backdrop blur tokens and their utility classes. Source:
`src/tokens/raw/blur.css`.

<BlurGrid vars={tokens.blur.vars} />

## Tokens

<TokenTable vars={tokens.blur.vars} preview="none" />

## Utilities

<table style={{ width: '100%', borderCollapse: 'collapse', marginBlock: 16 }}>
  <thead>
    <tr>
      <th style={{ textAlign: 'left', padding: '8px 12px', borderBottom: '1px solid #e2e8f0' }}>Class</th>
      <th style={{ textAlign: 'left', padding: '8px 12px', borderBottom: '1px solid #e2e8f0' }}>Declaration</th>
    </tr>
  </thead>
  <tbody>
    {tokens.blur.utilities.map((u) => (
      <tr key={u.selector}>
        <td style={{ padding: '8px 12px', fontFamily: 'monospace', fontSize: 13, borderBottom: '1px solid #e2e8f0' }}>{u.selector}</td>
        <td style={{ padding: '8px 12px', fontFamily: 'monospace', fontSize: 13, borderBottom: '1px solid #e2e8f0' }}>
          {Object.entries(u.declarations).map(([k, v]) => `${k}: ${v}`).join('; ')}
        </td>
      </tr>
    ))}
  </tbody>
</table>
