# Personal access token for the Figma REST API.
# Generate at https://www.figma.com/settings (Personal access tokens).
# Required scopes: File content (Read), Variables (Read).
# Used by `pnpm tokens:extract`.
FIGMA_TOKEN=
